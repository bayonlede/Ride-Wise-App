# API URL - Required for frontend to connect to backend (use NEXT_PUBLIC_ for client-side)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Public app URL (for SEO metadata)
NEXT_PUBLIC_APP_URL=http://localhost:3000
